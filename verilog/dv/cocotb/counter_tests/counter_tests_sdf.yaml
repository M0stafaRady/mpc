---
# Yalm file contain general design information that would mostly need to be updated in the first run only 
Tests: 
    - {name: counter_la, sim: GL_SDF}
    - {name: counter_wb, sim: GL_SDF}
    # - {name: counter_la_reset, sim: GL_SDF} can't run with GL because it has hdl path access
    - {name: counter_la_clk, sim: GL_SDF}

